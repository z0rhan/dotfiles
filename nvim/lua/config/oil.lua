require("oil").setup({
    columns = {
        "icon",
        "permissions",
        "size",
        -- "mtime",
    },

    delete_to_trash = false,

    view_options = {
        show_hidden = true,
    },
})
